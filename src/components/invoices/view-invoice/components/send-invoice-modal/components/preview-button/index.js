import React from "react";
import Styles from "./preview-button.module.scss";

const PreviewButton = (props) => {
  return <button className={Styles.btn}>Preview Invoice</button>;
};

export default PreviewButton;
