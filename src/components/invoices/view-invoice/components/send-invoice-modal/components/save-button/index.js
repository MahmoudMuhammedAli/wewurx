import React from "react";
import Styles from "./save-button.module.scss";

const SaveButton = (props) => {
  return <button className={Styles.btn}>Send Invoice</button>;
};

export default SaveButton;
