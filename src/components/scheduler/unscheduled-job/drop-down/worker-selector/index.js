import React from "react";
import Styles from "./worker-selector.module.scss";

const WorkerSelector = (props) => {
  return <div className={Styles.selector}>Selector goes here...</div>;
};

export default WorkerSelector;
