import React from "react";
import Styles from "./staffrates.module.scss";

const StaffRates = (props) => {
  return (
    <div className={Styles.rates}>
      <h4 className={Styles.rates__heading}>Staff rates</h4>
    </div>
  );
};

export default StaffRates;
