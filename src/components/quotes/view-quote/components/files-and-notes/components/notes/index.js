import React from "react";
import NotesDirectory from "../../../../../../shared/notes/notes-directory";
import Styles from "./notes.module.scss";

const Notes = (props) => {
  return (
    <div>
      <NotesDirectory />
    </div>
  );
};

export default Notes;
