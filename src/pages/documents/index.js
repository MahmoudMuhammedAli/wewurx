import React from 'react';
import Styles from './documents.module.scss';

const Documents = props => {
    return <div className = {`${Styles.documents} page`}> documents</div>
}
export default Documents;